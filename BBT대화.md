# BBT O/X 자동클릭 프로젝트 — 프로젝트 노트

마지막 업데이트: 2026-08-25

---

## 프로젝트 한줄 요약

BBT 프로그램 화면에서 O/X 심볼을 YOLOv26으로 감지 → Python으로 자동클릭

---

## 현재 상태 요약 (2026-08-24 기준) — 세션 시작 시 가장 먼저 읽을 것

- `auto_click_debug.py` 구현 완료, 실동작 검증 완료 (F9 캡처→추론→클릭, 영역 드래그 설정, 이미지 폴더 시뮬레이션 모두 정상 확인됨)
- 사용 중 가중치: `best_260707.pt` (Phase 1 — O_BLACK / X_BLACK / Ban)
- 실행 환경: **듀얼 모니터**, GPU(`cuda:0`) 사용, conf=0.3, imgsz=640 (`settings_autoclicker.json` 참고)
- 2026-08-24에 **치명적 버그 2건을 수정**했고, 그 결과 과거(260610~260708) "실전 검출률이 매우 낮다"는 기록의 상당 부분이 **버그로 인한 착시였음이 재검증으로 확인됨**. 아래 "알려진 버그 수정 이력"과 "과거 성능 평가 재해석" 섹션을 반드시 먼저 읽고, 옛 기록의 수치(재학습/데이터 재조정 관련)를 그대로 신뢰하지 말 것.
- 2026-08-25 재검증: `best_260707.pt` + **conf=0.3 (기본값, 0.05 임시방편 아님)**으로 시뮬레이션 → 결과 폴더 `dataset/valid/images/sim_results 4차 0707pt 0.3cof` 에서 정상 검출률 확인됨. **conf=0.05까지 낮출 필요가 없었다는 것도 함께 확정됨** — 이제 conf=0.3을 기본값으로 신뢰하고 사용.
  - 실측 로그: 12장 처리, **X_BLACK 113개 검출 (장당 평균 ~9.4개)**, O_BLACK 99개, Ban 1개.
  - 참고로 버그 수정 전(260703) 기록은 237장에서 X_BLACK 11개(장당 평균 ~0.05개) — 테스트셋이 달라 1:1 비교는 아니지만, 장당 검출 밀도가 자릿수 단위로 달라진 것은 버그가 원인이었다는 결론과 부합.

---

## 최종 확정된 클래스 구성

| 클래스명 | 설명 | 클릭 여부 | Phase |
|----------|------|-----------|-------|
| O_BLACK | 검정 배경 위 노랑 dot (빨강 링 있음) | 클릭 안함 | 1 |
| X_BLACK | 검정 배경 위 노랑 X cross | 즉시 클릭 | 1 |
| Ban | 비정형 도형 | 클릭 안함 | 1 |
| O_RED | 빨강 배경 위 노랑 dot (빨강 링 있음) | 조건부 (Phase 2) | 2 |
| X_RED | 빨강 배경 위 노랑 X cross | 조건부 (Phase 2) | 2 |

---

## 심볼 시각적 특징 (라벨링/디버깅 참고용)

### O 심볼 (O_BLACK / O_RED)
- 모양: 작은 **노랑 dot (점)** — Circle이 아님
- 테두리: 얇은 빨강 링이 dot 주변을 감쌈
- 주의: 큰 연두/올리브 원(대형 PCB pad)과 혼동하지 말 것 → 그것은 O가 아님

### X 심볼 (X_BLACK / X_RED)
- 모양: 노랑 X cross (십자형)
- O와 X는 **색상이 동일** (둘 다 노란색) → 형태로만 구분
- X_BLACK: 12·3·6·9시 방향 갭에 빨강색이 채워짐 → **색상 정보가 클래스 구분에 실제로 쓰이는 특징** (아래 채널 버그가 X_BLACK에 유독 치명적이었던 이유)

### BLACK vs RED 구분 기준
- 배경색만 다름 (검정 vs 빨강) → 색상으로 구분, YOLOv26의 형태+배경 학습에 의존

---

## 핵심 설계 결정사항

1. **가중치 분리 학습**: 가중치#1(Phase 1: O_BLACK/X_BLACK/Ban), 가중치#2(Phase 2: O_RED/X_RED)를 별도 학습·별도 파일로 운용. 통합 학습 시 BLACK/RED 혼동 경험이 있어 분리가 안전.
2. **OpenCV 후처리는 폐기**: Phase 2에서 HSV/Open-Close 방식 시도했으나 실패(빨강 배경이 외부와 픽셀 연결되어 판단 불가) → YOLOv26 분리 클래스 학습으로 완전 대체.

---

## 라벨링 요령 (확정 — 여전히 유효)

### bbox 크기
```
전 클래스 공통: 심볼 + 배경 2~3px 포함
- 너무 tight: 배경색 정보 없음 → BLACK/RED 구분 어려움
- 너무 loose: 이웃 심볼이 bbox 안에 들어옴 → 혼동
```

### 가장 중요한 규칙
> **라벨 누락이 가장 치명적.** bbox 크기보다 전수 라벨이 우선.
> 미라벨된 심볼은 YOLO 학습 시 "배경"으로 역학습되어 모델이 해당 패턴을 못 잡게 됨.

### Ban 라벨링
- 클릭 예외이며 발생 빈도가 낮음 → 데이터 부족은 구조적 문제, 점진적으로 보강

---

## 클릭 규칙 (확정, 코드와 일치)

| 클래스 | 동작 |
|--------|------|
| X_BLACK 감지 | 즉시 클릭 → BBT가 O_BLACK으로 변환 |
| O_BLACK 감지 | 클릭 안함 (시각화만) |
| Ban 감지 | 클릭 안함 (시각화만) |

- 클릭 후 대기시간 없음, 한 추론 pass에서 감지된 X_BLACK 전체 일괄 클릭
- 클릭 라이브러리: `pydirectinput` (`PAUSE=0` → 클릭이 사람 눈에 안 보일 정도로 즉시 실행됨)

---

## 실행 흐름 (현재 구현, F9 방식)

```
최초 1회:  [영역 설정] 버튼 → 화면 위에서 마우스 드래그 → cap_region 저장 (settings.json에 영구 저장)
시작:      [▶ 시작] 클릭 → 전역 F9 단축키 활성화 (keyboard 라이브러리)
반복:      F9 누를 때마다 1회 실행 — 저장된 영역 캡처 → YOLO 추론 → X_BLACK 전체 일괄 클릭
정지:      [■ 정지] 버튼 클릭 (마우스로만 정지, ESC 아님)
```

- F9는 **연속 루프가 아니라 누를 때마다 1회 실행**임에 주의 (연속 자동클릭 아님)
- 메인 스레드는 GUI만 담당, 캡처/추론/클릭은 워커 스레드 + `queue` 통신 (화면 멈춤 방지)

---

## settings_autoclicker.json 실제 구조 (코드 `DEFAULT_SETTINGS` 기준)

```json
{
  "weight_path": "D:/VIBE/BBT/best_260707.pt",
  "device": "cuda:0",
  "conf": 0.3,
  "iou": 0.45,
  "imgsz": 640,
  "click_order": "top_left",
  "capture_region": {"x": 2220, "y": 667, "w": 1599, "h": 774},
  "window_geometry": "815x1072+658+645",
  "always_on_top": true,
  "dark_mode": false,
  "hotkey": "f9",
  "sim_folder": "..."
}
```
- `capture_region.x`가 2220처럼 큰 값인 것은 **듀얼 모니터에서 두 번째 모니터 좌표**이기 때문 — 정상.

---

## 알려진 버그 수정 이력 (중요 — 향후 코딩 시 반드시 참고)

### 1. RegionSelector 멀티모니터 커버리지 버그 (수정 완료, 2026-08-24)

- **증상**: "영역 설정" 후 드래그해도 영역이 저장되지 않고 시작 시 "캡처 영역 설정 필요" 팝업이 계속 뜸.
- **원인**: `RegionSelector`가 Tkinter `-fullscreen` 속성을 썼는데, 이 속성은 보통 **창이 떠 있는 모니터 1개만** 덮는다. 듀얼 모니터 환경(이 프로젝트의 실제 환경)에서 오버레이가 덮지 않는 영역에서 드래그하면 Tkinter가 이벤트 자체를 받지 못함.
- **수정**: `ctypes windll.user32.GetSystemMetrics`로 가상 화면 전체 크기/좌표(`SM_XVIRTUALSCREEN` 등 76~79)를 구해 `self.geometry(f"{vw}x{vh}+{vx}+{vy}")`로 모든 모니터를 합친 영역을 직접 지정하도록 변경.
- **주의**: 앞으로 전체화면 오버레이가 필요한 기능을 추가할 때 `-fullscreen` 속성을 다시 쓰지 말 것 — 반드시 가상 화면 크기 기반으로 geometry를 직접 지정해야 멀티모니터에서 안전함.

### 2. YOLO 추론 시 BGR/RGB 채널 이중 반전 버그 (수정 완료, 2026-08-24) — 가장 중요

- **증상**: 학습에 쓴 원본 이미지 + 그 이미지로 학습한 가중치를 그대로 시뮬레이션에 넣어도 detection이 거의 안 나옴. (F9 캡처 실행에서도 동일하게 감지율이 비정상적으로 낮았음)
- **원인**: Ultralytics YOLO는 numpy 배열을 `model()`에 넘기면 **입력이 BGR(cv2.imread 기본 포맷)이라고 가정**하고 내부에서 자체적으로 BGR→RGB 변환을 한 번 수행한다. 그런데 이 코드는 `cv2.cvtColor(..., COLOR_BGR2RGB)`로 미리 RGB로 바꾼 이미지를 넘기고 있었음 → 채널이 **두 번** 뒤집혀 실제로는 채널이 반전된 이미지가 모델에 입력됨.
- **영향이 X_BLACK에 유독 컸던 이유**: X_BLACK의 핵심 구분 특징이 "12·3·6·9시 갭의 빨강 채움"(색상 의존적 특징)이라, 채널 반전이 정확히 이 단서를 왜곡시킴. O_BLACK은 형태(dot+링) 위주라 상대적으로 덜 망가지고 오히려 오탐이 늘어남 — 260708 기록의 "X_BLACK 심하게 누락, O_BLACK 오탐 다수" 패턴과 정확히 일치.
- **수정**: `self.model(...)` 호출에는 **BGR 원본**(`img_bgr`)을 그대로 전달하고, RGB 변환본(`img_rgb`)은 화면 시각화(`annotated`, 프리뷰)용으로만 사용하도록 분리. `_do_capture_and_click`과 `SimulationWindow._run` 양쪽 다 수정.
- **주의**: 앞으로 YOLO `model(...)` 호출부를 건드릴 때, numpy 배열은 **BGR로 넘겨야 한다**는 원칙을 반드시 지킬 것. RGB로 미리 변환한 배열을 절대 그대로 넘기지 말 것 (이중 반전 재발).
- **검증**: 2026-08-24, 기존 가중치 `best_260707.pt`를 고쳐진 파이프라인으로 재실행한 결과 `dataset/valid/images/sim_results` 등에서 기대한 수준의 정상 검출률 확인 → 버그가 원인이었음이 실측으로 확정됨.

---

## 과거 성능 평가 재해석 (260610~260708 기록에 대한 정정)

과거 세션(260703, 260708)에서 "mAP50=0.99인데 실전 검출은 11/237"이라는 극단적 불일치를 발견하고, 이를 **데이터 다양성 부족 / 과적합 / 클래스 불균형** 문제로 진단했었다. 하지만:

- `mAP50=0.99`는 `yolo val` (Ultralytics 자체 데이터로더 사용, 채널 처리 정상)로 측정된 신뢰 가능한 수치였음.
- 반면 "실전 검출 11/237"은 위 버그 #2가 있던 `auto_click_debug.py` 시뮬레이션으로 측정된 수치였음.
- 2026-08-24 재검증(버그 수정 후 `best_260707.pt` 재실행)으로, 이 가중치가 원래부터 상당히 쓸만했다는 것이 확인됨.

**따라서 다음 조치들은 버그로 인한 증상에 대한 과잉/오진단 대응이었을 가능성이 높으며, 그대로 신뢰하지 말 것:**

- O_BLACK 6760→3000, X_BLACK 2238→500 등 인위적 데이터 비율 재조정 계획
- ~~conf threshold를 0.05까지 낮추는 임시방편~~ → **2026-08-25 재검증 완료**: conf=0.3(기본값)에서 정상 검출 확인됨. 0.05로 낮출 필요 없었음.
- yolo26n → yolo26s 모델 업그레이드, hard example mining 계획 (버그 수정 전 진단을 근거로 세운 계획이므로 보류하고 재평가 필요)

**여전히 유효한 것 (버그와 무관한 일반 원칙):**

- 라벨 전수 작성 원칙, bbox 크기 가이드
- Phase 1/2 가중치 분리 학습 전략
- Ban 클래스 데이터 부족 문제 (구조적으로 발생 빈도가 낮아 여전히 보강 필요)

---

## data.yaml 예시 (Phase 1)

```yaml
names:
  0: Ban
  1: O_BLACK
  2: X_BLACK
nc: 3
train: ./train/images
val: ./valid/images
```

---

## 학습 명령어 참고 (재학습 필요 시)

```bash
yolo detect train \
  model=yolo26n.pt \
  data=./dataset/data.yaml \
  epochs=100 \
  imgsz=640 \
  batch=16 \
  name=phase1_ox_black \
  project=./runs
```

---

## Phase 2 (O_RED / X_RED) — 미착수

- Tab B는 GUI상 placeholder만 존재, 실제 추론/가중치 없음
- Phase 1이 검증된 지금 시점(버그 수정 완료) 기준으로 Phase 2 착수를 재검토할 수 있음
- O_RED는 노랑 dot+빨강링이 빨강 배경에 묻힐 가능성 있어 라벨링 시 주의 필요

---

## 다음 세션 시작 문구 예시

```
"BBT대화.md 복기했어. 정상 conf(0.3)에서 best_260707.pt 재검증 결과 공유할게: [결과]"
또는
"BBT대화.md 복기했어. Phase 1은 버그 수정 후 충분히 잘 나와서 재학습 불필요한 걸로 결론. Phase 2 착수할게."
또는
"BBT대화.md 복기했어. Ban 클래스 데이터 보강 라벨링 완료했어."
```
