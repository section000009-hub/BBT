# BBT O/X 자동클릭 프로젝트 — 세션 복기 노트

마지막 업데이트: 2026-06-10 (야간)

---

## 프로젝트 한줄 요약

BBT 프로그램 화면에서 O/X 심볼을 YOLOv26으로 감지 → Python으로 자동클릭

---

## 최종 확정된 클래스 구성

| 클래스명 | 설명                                 | 클릭 여부        | Phase |
| -------- | ------------------------------------ | ---------------- | ----- |
| O_BLACK  | 검정 배경 위 노랑 dot (빨강 링 있음) | 클릭 안함        | 1     |
| X_BLACK  | 검정 배경 위 노랑 X cross            | 즉시 클릭        | 1     |
| Ban      | 비정형 도형                          | 클릭 안함        | 1     |
| O_RED    | 빨강 배경 위 노랑 dot (빨강 링 있음) | 조건부 (Phase 2) | 2     |
| X_RED    | 빨강 배경 위 노랑 X cross            | 조건부 (Phase 2) | 2     |

---

## 심볼 시각적 특징 (이미지 분석 결과)

### O 심볼 (O_BLACK / O_RED)

- 모양: 작은 **노랑 dot (점)** — Circle이 아님
- 테두리: 사각 바운딩 박스 4개 모서리에는 검정색이 있으며, 얇은 빨강 링이 dot 주변을 감쌈\_260609
- 주의: 큰 연두/올리브 원(대형 PCB pad)과 혼동하지 말 것 → 그것은 O가 아님

### X 심볼 (X_BLACK / X_RED)

- 모양: 노랑 X cross (십자형)
- O와 X는 **색상이 동일** (둘 다 노란색) → 형태로만 구분

### BLACK vs RED 구분 기준

- O_BLACK vs O_RED: **배경색만** 다름 (검정 vs 빨강)
- X_BLACK vs X_RED: **배경색만** 다름 (검정 vs 빨강)
- → 색상으로 구분 불가, YOLOv26의 형태+배경 학습에 의존

---

## 핵심 설계 결정사항

### 1. 가중치 분리 학습

```
가중치#1 (Phase 1): O_BLACK, X_BLACK, Ban 전용 모델
가중치#2 (Phase 2): O_RED, X_RED 전용 모델

런타임: 조건에 따라 어떤 가중치 사용할지 코드에서 지정
```

### 2. 분리 학습 이유

- 통합 학습 시 X_BLACK ↔ X_RED 혼동 발생 경험 있음
- 배경색의 미묘한 차이를 하나의 모델이 구분하는 것은 불안정
- Phase 1 모델은 RED 변종이 없으므로 혼동 여지 자체가 없음

### 3. OpenCV 후처리 결정

- Phase 2에서 OpenCV HSV 방식 이미 실패 확인됨
  → 빨강 배경이 외부와 픽셀 연결(OPEN 구조)이어서 Close/Open 판단 실패
- Phase 2 전략: YOLOv26 분리 클래스 학습으로 완전 대체

---

## 라벨링 요령 (확정)

### bbox 크기

```
전 클래스 공통: 심볼 + 배경 2~3px 포함
- 너무 tight: 배경색 정보 없음 → BLACK/RED 구분 어려움
- 너무 loose: 이웃 심볼이 bbox 안에 들어옴 → 혼동
```

### O_BLACK 라벨링

- 노랑 dot + 빨강 링 전체 포함
- dot+링 주변 2~3px 배경 포함
- 큰 PCB pad(대형 원)는 라벨 대상 아님

### X_BLACK 라벨링

- X cross 전체 포함
- 주변 2~3px 배경 포함
- 노랑 X 모양의 Gap\_틈 : 12시 & 3시 & 6시 & 9시 방향에는 빨강색이 채운 부분이 있음\_260609
- 바운딩 박스안쪽 최외곽은 Black 배경색을 포함함\_260609

### Ban 라벨링

- 비정형 도형 전체 포함
- 클릭 예외이며, 소수 발생되므로 자동화가 긴급하지 않음
- 점진적으로 학습 이미지 추가할 예정

### 가장 중요한 규칙

> **라벨 누락이 가장 치명적.** bbox 크기보다 전수 라벨이 우선.
> 이전 실패 원인: 산발적 심볼 미라벨 → False Negative 누적

---

## Roboflow 작업 순서

```
[ ] 1. 클래스명 remapping
       O_1 (또는 O)  → O_BLACK
       X_1 (또는 X)  → X_BLACK
       Ban            → Ban
       O_RED, X_RED   → 이름만 지정, Phase 2에서 라벨링

[ ] 2. Phase 1 라벨링 전수 재작업
       - O_BLACK: 전 이미지 dot 전부 라벨
       - X_BLACK: 전 이미지 X 전부 라벨
       - Ban: 전 이미지 Ban 전부 라벨
       - O_RED, X_RED: 이번 Phase 1에서는 라벨 Skip

[ ] 3. Export
       Format: YOLOv8 선택 (YOLOv26 호환됨)
       클래스 필터: O_BLACK, X_BLACK, Ban 만 포함
       data.yaml 클래스 순서 확인
```

---

## 학습 환경 및 파일 현황

| 항목                     | 상태                                                                          |
| ------------------------ | ----------------------------------------------------------------------------- |
| yolo26n.pt               | 로컬 다운로드 완료                                                            |
| 데이터셋 (Phase 1)       | 수집 완료, 추가 라벨링 예정                                                   |
| **가중치#1**             | **학습 완료** — `runs/detect/runs/phase1_ox_black_260609_use/weights/best.pt` |
| 가중치#2                 | 미학습 (Phase 2)                                                              |
| **auto_click_debug.py**  | **코딩 완료, 시뮬레이션 테스트 완료**                                         |
| debug_mask_checker_v2.py | 미작성                                                                        |

---

## 학습 명령어 (Phase 1 기준)

```bash
yolo detect train \
  model=yolo26n.pt \
  data=./dataset/data.yaml \
  epochs=100 \
  imgsz=640 \
  batch=16 \
  name=phase1_ox_black \
  project=./runs
```

학습 완료 후 결과 위치:

```
./runs/phase1_ox_black/weights/best.pt      ← 가중치#1
./runs/phase1_ox_black/results.png
./runs/phase1_ox_black/confusion_matrix.png
```

---

## 런타임 구조 (최종 목표)

```
auto_click.py
  ├── model_phase1 = YOLOv26(가중치#1)   → O_BLACK, X_BLACK, Ban
  └── model_phase2 = YOLOv26(가중치#2)   → O_RED, X_RED  [Phase 2 이후]

매 프레임:
  1. phase1 추론 → X_BLACK 감지 → 즉시 클릭
  2. [Phase 2] phase2 추론 → 조건 판단 → 조건부 클릭
```

---

## data.yaml 예시 (Phase 1)

```yaml
names:
  0: Ban
  1: O_BLACK
  2: X_BLACK
nc: 3
train: ./train/images
val: ./valid/images
```

---

## 업로드된 라벨링 이미지 분석 요약 (5장)

파일명: `roboflow_annotation_#1~#5.png`  
위치: `d:\바탕화면\`

| 이미지 | 주요 특징                                                     |
| ------ | ------------------------------------------------------------- |
| #1     | 좌측 BOM 목록 패널 열린 상태. O/X 심볼 혼재. 라벨 일부만 있음 |
| #2     | 화면 하단부 중심. X 클러스터 소수 라벨. 산발 심볼 미라벨 다수 |
| #3     | 상단에 대형 X_RED 뚜렷하게 보임. O/X 혼재. 라벨 일부          |
| #4     | 우측 상단 X 클러스터 cyan 박스 다수. O dot 라벨 미확인        |
| #5     | 가장 많은 라벨 포함. 우상단 클러스터 집중. 중앙 좌측 미라벨   |

공통 관찰:

- **라벨 누락이 심각** — 화면 전체 대비 라벨된 비율 낮음
- bbox 크기 자체는 극단적으로 loose하지 않음
- O dot 전용 라벨박스는 이미지 확인 어려움 (미라벨 의심)

---

## Phase 2 전략 (미확정, 추후 진행)

```
현황: OpenCV HSV/Close/Open → 실패 확인
확정: YOLOv26 분리 클래스 학습 (전략 A)

O_RED: 노랑 dot + 빨강링이 빨강 배경에 묻힐 가능성 있음 → 라벨 시 주의
X_RED: cross 모양이 빨강 배경에서도 뚜렷 → 상대적으로 쉬움

Phase 2 진행 전 필요:
- Phase 1 학습 + 실전 안정 동작 확인 먼저
- 이후 O_RED/X_RED 라벨링 전략 재논의
```

---

## 세션 재개 시 시작 질문

```
"BBT대화.md 복기했어. 현재 [단계] 에서 막혔어."
또는
"Roboflow 라벨링 완료했어. data.yaml 내용이야: [붙여넣기]"
```

---

## 학습 결과 분석 — 260609 (phase1_ox_black_260609_use)

분석일: 2026-06-09  
결과 폴더: `./runs/detect/runs/phase1_ox_black_260609_use/`

### 학습 설정 요약

| 항목                | 값                                            |
| ------------------- | --------------------------------------------- |
| 모델                | yolo26n.pt (nano)                             |
| 설정 epoch          | 5000                                          |
| **실제 종료 epoch** | **213** (early stopping, patience=50)         |
| **Best epoch**      | **~163** (213 - 50 = 163)                     |
| Batch / imgsz       | 32 / 640                                      |
| 사용 가중치         | best.pt (epoch100, epoch200 중간 저장도 있음) |

### 핵심 지표 (Best epoch 기준)

| 지표               | 값        |
| ------------------ | --------- |
| **mAP@0.5 (전체)** | **0.669** |
| mAP@0.5-0.95       | 0.479     |
| Precision          | ~0.61     |
| Recall             | ~0.64     |

### 클래스별 mAP@0.5 (PR Curve 기준)

| 클래스      | mAP@0.5   | 평가                  |
| ----------- | --------- | --------------------- |
| **X_BLACK** | **0.847** | 우수 — 실전 투입 가능 |
| Ban         | 0.589     | 보통                  |
| O_BLACK     | 0.572     | 부족                  |

### Confusion Matrix 분석

```
X_BLACK  → 올바른 탐지 0.99  (FN 1%) — 거의 완벽
O_BLACK  → 올바른 탐지 0.47  (FN 53%) — 절반 이상 누락
Ban      → 올바른 탐지 0.45  (FN 55%) — 절반 이상 누락

False Positive:
  background → O_BLACK 오탐 0.65  ← 가장 심각한 문제
  background → X_BLACK 오탐 0.32
  background → Ban 오탐 0.03
```

### Loss 곡선 상태

- Train/Val Loss 모두 **단조 감소** → 학습 불안정 없음, 정상 수렴
- Val cls_loss > Train cls_loss 갭 존재 → 분류 부분에서 약한 과적합

### 원인 진단

- **O_BLACK**: 원형 dot 특성상 배경의 유사 패턴과 혼동. 데이터 부족 가능성
- **Ban**: 비정형 도형 특성 + 데이터 부족
- **X_BLACK**: X 형태가 시각적으로 가장 뚜렷 → 모델이 쉽게 학습

### 개선 방향 (우선순위순)

1. **O_BLACK / Ban 라벨 데이터 추가 수집** — 가장 급한 과제. 다양한 밝기/위치/거리 조건 필요
2. **Hard Negative Mining** — background를 O_BLACK로 오탐하는 케이스를 negative 예시로 추가 학습
3. **모델 업그레이드** — yolo26n → yolo26s 변경 시 Ban/O_BLACK 성능 향상 기대
4. **클래스 손실 가중치 조정** — O_BLACK, Ban에 더 높은 cls weight 부여 검토

### 결론 및 다음 단계

> X_BLACK는 실전 투입 가능 수준이나, O_BLACK와 Ban은 추가 데이터 없이 실사용 불가.  
> best.pt(epoch ~163) 사용. 다음 세션에서 O_BLACK/Ban 데이터 보강 후 재학습 예정.

```
다음 세션 시작 질문 예시:
"260609 결과 복기했어. O_BLACK 추가 라벨링 완료했어. data.yaml: [붙여넣기]"
```

---

## auto_click_debug.py 설계 확정 (260610)

설계 확정일: 2026-06-10  
코딩 시작: 추후 사용자 요청 시

---

### 클릭 규칙 (확정)

| 클래스       | 동작                               |
| ------------ | ---------------------------------- |
| X_BLACK 감지 | 즉시 클릭 → BBT가 O_BLACK으로 변환 |
| O_BLACK 감지 | 클릭 안함                          |
| Ban 감지     | 클릭 안함                          |

- 클릭 후 대기시간 없음 (BBT 프로그램이 즉시 모양 변환)
- 감지된 X_BLACK 전체 일괄 클릭 (한 추론 pass 기준)
- 클릭 라이브러리: `pydirectinput`

---

### 실행 환경

| 항목         | 값                                              |
| ------------ | ----------------------------------------------- |
| OS           | Windows 10                                      |
| GPU          | CUDA 사용 불가 (CPU-only 대상 PC)               |
| 모니터       | 24인치 1대                                      |
| BBT 프로그램 | 창(Window) 모드, HDI PCB 검사 장비 연동         |
| 화면 배치    | 자동클릭 GUI 좌측 ~30% / BBT 프로그램 우측 ~70% |

- GPU/CPU 겸용 (하나의 프로그램 내 토글) — 나중에 GPU PC에서도 재사용 가능

---

### GUI 구조 (세로형 레이아웃)

```
┌────────────────────────┐
│ [파일] [창 설정] [도움말] │  ← 메뉴바
├────────────────────────┤
│ [Tab A: Phase1▶] [Tab B: Phase2] │
├────────────────────────┤
│ ▣ 제어판                │
│  가중치: [📁 파일 선택]  │
│  파일명 표시줄           │
│  장치: ⊙CPU  ○GPU       │
│  conf  [슬라이더] 0.50  │
│  iou   [슬라이더] 0.45  │
│  imgsz [320▼ / 640]     │
│  클릭순서 [좌→우/위→아래▼] │
│  캡처영역: [영역 설정 버튼] │
│  [▶ 시작]  [■ 정지]     │
│  [📌 항상 위] [◑ 다크모드] │
├────────────────────────┤
│ ▣ 화면 프리뷰            │
│  (캡처 화면 + bbox 오버레이) │
├────────────────────────┤
│ ▣ 실행 로그              │
│  [HH:MM:SS] X_BLACK 3개 클릭 │
│  [HH:MM:SS] 감지: X×3 O×5 │
└────────────────────────┘
  폭: ~480px  |  항상 위 옵션
```

---

### 메뉴바 — [창 설정]

```
[창 설정]
  ├── 창 크기/위치 프리셋
  │     ├── 좌측 좁게  (380px × 전체높이)
  │     ├── 좌측 보통  (480px × 전체높이)  ← 기본값
  │     └── 좌측 넓게  (600px × 전체높이)
  ├── 현재 크기/위치 저장
  └── 위치 초기화 (좌상단)
```

---

### 캡처 방식 (혼합 방식 — 확정)

```
최초 1회:  [영역 설정] 버튼 → BBT 창 위에서 마우스 드래그 → 좌표 저장
이후 반복:  F9 누름 → 저장된 영역 캡처 → YOLO 추론 → X_BLACK 전체 클릭
재시작 시:  settings.json에서 영역 자동 복원 (드래그 불필요)
```

---

### 단축키

| 키             | 동작                            |
| -------------- | ------------------------------- |
| F9             | 캡처 → 추론 → X_BLACK 일괄 클릭 |
| 추후 추가 가능 | 사용자 요청 시                  |

---

### settings.json 저장 항목

```json
{
  "weight_path": "runs/detect/.../best.pt",
  "device": "cpu",
  "conf": 0.5,
  "iou": 0.45,
  "imgsz": 320,
  "click_order": "top_left",
  "capture_region": { "x": 400, "y": 0, "w": 1000, "h": 1080 },
  "window": { "x": 0, "y": 0, "w": 480, "h": 1080 },
  "always_on_top": true,
  "dark_mode": false
}
```

---

### 아키텍처

```
메인 스레드:   tkinter GUI (여기서 직접 추론 금지 — 화면 멈춤 발생)
워커 스레드:   F9 이벤트 수신 → 캡처 → YOLO 추론 → pydirectinput 클릭
스레드 통신:   queue (로그 메시지, 프리뷰 프레임 전달)
```

---

### requirements.txt 예정 구성

```
ultralytics
mss
pydirectinput
opencv-python
Pillow
numpy
pywin32
```

---

### 코딩 후 시뮬레이션 테스트 계획

```
BBT 이미지 폴더 지정
  ↓
best.pt 로드 → 각 이미지 추론
  ↓
가상 클릭 포인트 계산 (X_BLACK bbox 중심)
  ↓
결과 이미지 저장 (클릭 위치 오버레이 표시)
  ↓
리포트:
  - 이미지당 X_BLACK 클릭 예정 좌표
  - O_BLACK / Ban 오클릭 여부 체크
  - 오탐(FP) 의심 케이스 표시
```

- 사용자가 이미지 폴더 준비 중
- 코딩 완료 후 폴더 경로 지정하여 실행 예정

---

### 다음 세션 시작 문구

```
"BBT대화.md 복기했어. auto_click_debug.py 코딩 시작해줘."
또는
"BBT대화.md 복기했어. 시뮬레이션 폴더 준비됐어: [경로]"
```

---

## auto_click_debug.py 구현 완료 및 시뮬레이션 결과 (260610)

### 구현 완료 항목

| 항목                                           | 상태    |
| ---------------------------------------------- | ------- |
| 세로형 GUI (제어판→프리뷰→로그)                | ✔ 완료 |
| Tab A Phase1 / Tab B Phase2(placeholder)       | ✔ 완료 |
| 가중치 선택 + 자동 재로드                      | ✔ 완료 |
| F9 전역 단축키 (시작 시만 활성)                | ✔ 완료 |
| X_BLACK 일괄 클릭 (pydirectinput)              | ✔ 완료 |
| 캡처 영역 1회 드래그 설정 + settings.json 저장 | ✔ 완료 |
| 이미지 폴더 시뮬레이션 (메뉴 → 파일)           | ✔ 완료 |
| 다크/라이트 모드, 항상 위, 창 크기 프리셋      | ✔ 완료 |

파일 위치: `auto_click_debug.py` (프로젝트 루트)  
설정 저장: `settings_autoclicker.json` (자동 생성)

---

### 버그 발견 및 수정: cv2 한글 경로 문제

**증상**: 시뮬레이션 실행 시 이미지 로드 실패 → 탐지 0개 → bbox 없는 빈 이미지 저장

**원인**: `cv2.imread()`가 한글 경로에서 항상 `None` 반환 (Windows OpenCV 알려진 이슈)

```python
# 수정 전 (실패)
img = cv2.imread(path)

# 수정 후 (한글 경로 대응)
buf = np.fromfile(path, dtype=np.uint8)
img = cv2.imdecode(buf, cv2.IMREAD_COLOR)

# 저장도 동일하게
_, buf = cv2.imencode(".jpg", img_bgr)
with open(save_path, "wb") as f:
    f.write(buf.tobytes())
```

→ 수정 후 정상 동작 확인. **한글 경로를 포함하는 모든 이미지 작업에 동일 패턴 적용 필요.**

---

### 시뮬레이션 실행 결과 (260610)

```
대상 폴더: D:\VIBE\데이터 셋\3차 라벨링_260609\...train\images
처리 이미지: 237 / 237장
conf = 0.50 기준

X_BLACK 클릭예정: 41개  (이미지당 평균 0.17개)
O_BLACK 감지:     892개 (이미지당 평균 3.76개)
Ban 감지:          8개
```

---

### 시뮬레이션 결과 분석 — 검출 부족 원인 규명

**① 동일 원본 증강 문제**

```
학습 이미지 237장 = 원본 1장(20260331_1651_07_png) × Roboflow 증강
→ 모델이 학습한 BBT 화면: 사실상 1가지 줌 레벨
→ 다른 줌/위치에서 촬영한 X_BLACK → 미감지
```

**② X_BLACK 라벨 누락 (가장 치명적 원인)**

```
실제 라벨링 방식: X_BLACK 10개 있으면 5개만 라벨 (피로도로 인해)
YOLO 해석:
  - 라벨된 5개  → "이것이 X_BLACK"  ← 학습
  - 미라벨 5개  → "이것은 배경"     ← 역학습 !!

결과: 모델이 특정 X_BLACK 패턴을 배경으로 학습 → 미감지
```

**③ bbox 크기 불일치**

```
Roboflow 라벨: X 심볼 전체 + 배경 여백 포함 (큰 bbox)
Detection 결과: X 심볼 중심 일부만 잡힘 (작은 bbox)
→ 모델이 X_BLACK의 특징 영역을 일관되게 학습하지 못함
```

**④ X_BLACK 시각적 특성**

```
X_BLACK: 12·3·6·9시 방향 갭에 빨강 채움
O_BLACK: 노랑 dot + 빨강 링
→ 두 클래스 모두 빨강 요소 포함 → 작은 bbox에서 혼동 발생
```

---

### 원본 이미지만으로 학습 시도 검토

원본 10장 이상 존재 확인됨. 아래 기준으로 판단:

| 조건                        | 예상 결과            |
| --------------------------- | -------------------- |
| 원본 10장 + 50% 라벨 (현재) | ❌ 현재와 비슷       |
| 원본 10장 + 100% 라벨       | ✔ 확실한 개선       |
| 원본 3장 + 100% 라벨        | ✔ 10장 50%보다 나음 |

→ **데이터 수보다 라벨 완전성이 우선**

---

### 다음 세션 작업 방향 (우선순위순)

```
① [최우선] X_BLACK 이미지 2~3장 선택 → 전수 완전 라벨링 (100%)
   - X_BLACK 10개 있으면 10개 전부 라벨
   - bbox 크기: X cross 전체 + 주변 배경 3~5px 포함 균일하게

② 다양한 줌/위치 BBT 스크린샷 5~10장 추가 캡처
   - BBT 프로그램 이동/줌 변경 후 스크린샷

③ Roboflow 증강 없이 원본으로 재학습 (빠른 실험)
   - 완전 라벨된 이미지만으로 yolo26n 재학습
   - 결과를 260609 결과와 비교

④ 결과 개선 확인 후 증강 추가하여 최종 학습
```

**다음 세션 시작 문구:**

```
"BBT대화.md 복기했어. X_BLACK 완전 라벨링 완료했어. 재학습 진행해줘."
또는
"BBT대화.md 복기했어. BBT 스크린샷 추가했어. data.yaml: [붙여넣기]"
```

---

## Phase 1 모델 평가 및 재학습 계획 (260703)

### 세션 진행 내용

**2026-07-03 진행:**

1. auto_click_debug.py 시뮬레이션으로 Phase 1 (mAP50=0.99) 모델 실제 성능 평가
2. 로그와 이미지 결과 불일치 발견 및 코드 디버그
3. confidence threshold 조정 (0.50 → 0.20 → 0.15 시도)
4. 데이터 다양성 분석을 통한 근본 원인 파악

---

### 실제 성능 평가 결과 (충격적 발견)

#### 평가 지표 vs 실제 성능

| 항목                       | 값         | 평가            |
| -------------------------- | ---------- | --------------- |
| **mAP50 (Validation Set)** | 0.99       | ✓ 높음          |
| **실제 X_BLACK 검출**      | 11개/237장 | 🔴 매우 낮음    |
| **X_BLACK Confidence**     | 0.2~0.3    | 🔴 매우 불안정  |
| **O_BLACK 검출**           | 5117개     | ✓ 정상          |
| **로그 vs 이미지**         | 불일치     | 🔴 카운팅 오류? |

#### 학습 데이터 구성 분석

```
총 인스턴스:
- Ban:      220개 (3.2%)
- O_BLACK: 6760개 (97.5%) ← 과다
- X_BLACK: 2238개 (32.3%) ← 부족

클래스 불균형:
O_BLACK : X_BLACK = 3 : 1 (심각한 불균형)

원본 이미지 다양성:
- 원본 개수: 227~237장 (충분함)
- 클래스 내 패턴: 3~5가지 반복 (매우 부족) ← 핵심 문제!
```

#### 문제점 진단

```
mAP50 = 0.99인데 왜 X_BLACK을 못 찾나?

원인:
1. 데이터 다양성 부족
   - 원본 이미지는 237장 다르지만
   - X_BLACK은 3~5가지 패턴만 반복
   - Roboflow 증강으로 240배 확대 (무의미)

2. 모델 과적합
   - 특정 패턴(1~2가지)에만 높은 신뢰도
   - 다른 패턴 → 미탐 또는 O_BLACK으로 오분류

3. 학습/테스트 도메인 불일치
   - Validation Set: 특정 조건만 (mAP 높음)
   - Test Set: 다양한 조건 (실제 성능 낮음)

4. BBT 프로그램의 특성
   - 고정 모니터 (24인치, 5년전)
   - 고정 조명, 위치, 각도 → 줌/조명 변수 없음
   → 클래스 비율 조정이 가장 효과적
```

---

### 해결 방안: 클래스 비율 재조정 + 재학습

#### 전략 (Option A: 빠른 개선)

```
현재 데이터:
- O_BLACK: 6760개
- X_BLACK: 2238개
- Ban: 220개

변경 전략:
- O_BLACK: 6760 → 3000개 (50% 감소)
- X_BLACK: 2238 → 500개 (75% 감소, 품질 우선)
- Ban: 220 → 유지

목표:
X_BLACK : O_BLACK = 1 : 6 (이전 1 : 3 → 더 균형잡힘)

기대 효과:
- X_BLACK 검출: 11개 → 50~100개 (4~9배 개선)
- Confidence: 0.2~0.3 → 0.4~0.6 (안정성 개선)
- O_BLACK 오분류 감소
```

#### O_BLACK 선별 기준

**방법: 이미지별 균등 샘플링 (추천)**

```
1. 각 이미지의 O_BLACK 개수 파악
2. 비율: 3000 / 6760 ≈ 0.44 (44% 유지)
3. 각 이미지의 O_BLACK 44%만 선택
4. 결과: O_BLACK 3000개로 축소

또는 더 간단한 방법:
- O_BLACK이 많은 상위 150개 이미지만 선택
- 자동으로 O_BLACK/X_BLACK 비율 최적화
```

#### 데이터 재구성 계획

```
현재 데이터셋:
data.yaml (train/val split 유지)
├─ train/images: 233개
├─ train/labels: O_BLACK 6760 + X_BLACK 2238
├─ val/images: 33개
├─ val/labels: (검증용, 변경 X)

변경 후:
train/labels 재구성 (O_BLACK만 선별)
├─ O_BLACK: 6760 → 3000 (상위 이미지 기준)
├─ X_BLACK: 2238 → 500~700 (유지 또는 약간 감소)
└─ 라벨 파일 업데이트

변경 미적용:
✓ val/labels (검증 성능 비교용)
✓ val/images (변경 X)
```

---

### 코드 개선 사항

#### auto_click_debug.py 시뮬레이션 개선

**개선 1: 모든 클래스 시각화**

```python
# 이전: X_BLACK만 마커 표시
# 개선: 모든 클래스에 서로 다른 마커
- X_BLACK: 초록 X자 마커 + "X[N]" 번호
- O_BLACK: 주황 원 마커 + "O[N]" 번호
- Ban: 파랑 별 마커 + "B[N]" 번호
```

**개선 2: GUI 리스트에 모든 클래스 개수 표시**

```python
# 이전: [X]×5  image.jpg
# 개선: X 5 O 53 B 0  |  image.jpg

→ 각 이미지별 O_BLACK/Ban 개수도 실시간 확인 가능
```

**개선 3: settings_autoclicker.json 설정**

```json
{
  "weight_path": "D:/VIBE/.../best_260629.pt",
  "device": "cuda:0",
  "conf": 0.2,
  "iou": 0.45,
  "imgsz": 640,  ← 학습과 일치 (중요!)
  ...
}
```

---

### Early Stopping 분석

```
설정: patience=100
Best epoch: 361 (epoch 460 중)
종료: epoch 461 (361 + 100)

평가:
✓ patience=100은 정상 설정
✓ 모든 이미지가 461번 학습됨 (앞부분만 학습 X)
✓ 변경 불필요
```

---

### 다음 단계 (우선순위순)

#### Phase 1 재학습 (1주일 계획)

```
□ Step 1: 데이터 선별 (1일)
   - O_BLACK 6760 → 3000 선별
   - X_BLACK 2238 → 500~700 확인
   - 스크립트: PowerShell 또는 Python으로 자동화

□ Step 2: Roboflow 재설정 (1일)
   - 증강 제외 (또는 최소화)
   - 새 버전 export
   - data.yaml 재확인

□ Step 3: 재학습 실행 (2일)
   epochs=5000, patience=100
   name=phase1_ox_black_balanced_260703

□ Step 4: 시뮬레이션 평가 (1일)
   - X_BLACK 검출 개수 확인 (11 → 50~100?)
   - Confidence 분포 확인 (0.2~0.3 → 0.4~0.6?)
   - O_BLACK 오분류 감소 확인

□ Step 5: 결과 비교 분석 (1일)
   - 이전 모델 vs 새 모델 (confusion matrix)
   - mAP50 변화 추적
   - 최종 가중치 선택
```

#### Phase 2 준비 (이후)

```
현재: 미준비 상태
계획:
□ O_RED, X_RED 데이터 수집
□ 같은 방식으로 라벨링
□ Phase 2 모델 학습
```

---

### 다음 세션 시작 문구

```
"BBT대화.md 복기했어. 데이터 비율 조정해서 재학습할 준비됐어."
또는
"O_BLACK 3000 / X_BLACK 500으로 선별 완료했어. 재학습 시작해줘."
또는
"시뮬레이션 결과: X_BLACK 11 → 몇 개 나왔어?"
```
